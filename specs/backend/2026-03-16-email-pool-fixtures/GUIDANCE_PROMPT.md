Read [SPEC.md](/Users/lishehao/Desktop/Project/CalendarDIFF/specs/backend/2026-03-16-email-pool-fixtures/SPEC.md) first.

Implement the private email-pool fixture architecture exactly as specified.

Constraints:

1. stay backend-only
2. keep all collected Gmail data under `tests/fixtures/private/email_pool/`
3. do not touch frontend
4. use the current Gmail OAuth source already available in this repo
5. normalize the synthetic positive set into the shared sample schema

Before finishing:

1. write what you changed into `OUTPUT.md`
2. include exact file paths
3. include validation commands and counts per bucket
4. end with a short Chinese summary
