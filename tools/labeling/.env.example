# Minimal local config for labeling tool.
# Copy to tools/labeling/.env and fill your values.
# Note: tools/labeling/.env is gitignored.

OPENAI_API_KEY=sk-REPLACE_ME
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-5.3-codex

# Everything else uses script defaults.
# If needed, override input/output/runtime via shell env when running.
